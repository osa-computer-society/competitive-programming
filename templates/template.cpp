// Problem ID: 
// By Alexander Cai

#include <iostream>
#define FOR(i, a, b) for (int i = (a); i <= (b); ++i)
using namespace std;
typedef long long ll;

int main()
{
    cin.sync_with_stdio(0);
    cin.tie(0);

    int N;
    cin >> N;

    FOR(i, 1, N)
    {
        // run code
    }

    return 0;
}
