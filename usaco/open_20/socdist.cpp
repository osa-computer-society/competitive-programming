// Problem ID: 
// By 

#include <iostream>
using namespace std;

int main()
{
    cin.sync_with_stdio(0);
    cin.tie(0);

    int N, M;
    cin >> N >> M;

    for (int i = 1, a, b; i <= M; i++)
    {
      cin >> a >> b;
    }

    return 0;
}
