// Problem ID: 
// By 

#include <iostream>
using namespace std;

int main()
{
    cin.sync_with_stdio(0);
    cin.tie(0);

    // 1. We choose an arbitrary node.
    // Each stage: compute the edge of minimum weight going from a vertex included in the tree to one outside.
    
    return 0;
}
